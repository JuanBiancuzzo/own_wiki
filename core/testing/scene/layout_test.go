package scene_test

import (
	"testing"
)

// ---+--- tests ---+---

func TestLayoutWithNoElements(t *testing.T) {
	// We would need to check that the layout with a given configuration
	// and inserted elements, the layout changes the Porcentage, EM,
	// Shrink and Spand to the Fix pixel base value, and are the correct
	// values for them
	t.Fail()
}
