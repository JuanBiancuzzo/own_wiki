package api_test

import (
	"testing"
)

// ---+--- tests ---+---

func TestNoObjectsDrawCommands(t *testing.T) {
	// Generate a scene with no objects, and call GenerateDrawCommands, then
	// we would send them view the ProtoBuff, and the receive. This will make
	// a better test for testing the scene via the api
	t.Fail()
}
