package core

import (
	"fmt"

	s "github.com/JuanBiancuzzo/own_wiki/core/scene"
)

func NewRenderRequest(frameInformation s.FrameInformation) (*RenderRequest, error) {
	return nil, fmt.Errorf("TODO: Change the idea of frame information")
}
